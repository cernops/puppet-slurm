#
# File managed by Puppet!
#
# PARTITIONS
#
<% 
scope.lookupvar('slurm::partitionlist').each do |part|
  partition = []
  scope.lookupvar('slurm::partition_keys').each do |key|
    partition << "#{key}=#{part[key]}" if part[key]
  end
-%>
<%= partition.join(" ") %>
<% end -%>

