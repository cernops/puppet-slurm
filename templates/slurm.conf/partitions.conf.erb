#
# File managed by Puppet!
#
# PARTITIONS
#
<% 
scope['slurm::partitionlist'].each do |part|
  partition = []
  scope['slurm::partition_keys'].each do |key|
    partition << "#{key}=#{part[key]}" if part[key]
  end
-%>
<%= partition.join(" ") %>
<% end -%>

